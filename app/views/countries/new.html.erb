<h1> New Country </h1>

<%= render partial: "layouts/error", locals: {object: @country} %>


<%= form_for(@country) do |f| %>
    <div>
        <%= f.text_field :name %>
    </div>
    <br><p><%= f.submit "Add Country"%></p>
<% end %>